

const initialState = {
        pageSize:20,
        totalItems:[],
        totalItemsCount:0,
        currentPage:1,
        currentItems:[]
}

export const pagination=(state=initialState, action)=>{
        switch (action.type) {
                default:
                        return state
        }
}